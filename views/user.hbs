<div class="row mt-5">
  <div  class="col">	
		<h1>Photoalbums of {{notAdminName}}</h1>
	{{#if check}}
	<div id="update">
		<ul id="ul" class="list-group">
	{{#if albums}}
     {{#each albums}}
        <li id="li-{{id}}" class="list-group-item">
          <a data-href={{id}} id="a-{{id}}" href="">{{this.title}}</a>
          <button data-edit={{id}} class="btn btn-primary" type="click">edit title</button>
          <button data-delete={{id}} class="btn btn-primary" type="click">delete</button>
        </li>	
				{{/each}}
				</ul>	
  </div>
</div>
<form id="userForm">
  <div class="mb-3">
    <label for="exampleInputEmail1" class="form-label">Title</label>
    <input type="text" name="title" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
  </div>
  <button id="addAlbum" type="submit" class="btn btn-primary">Add album</button>
</form>
{{else}}
</ul>
</div>
  </div>
</div>
<p>No albums Yet</p>

<form id="userForm">
  <div class="mb-3">
    <label for="exampleInputEmail1" class="form-label">Title</label>
    <input type="text" name="title" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
  </div>
  <button id="addAlbum" type="submit" class="btn btn-primary">Add album</button>
</form>
{{/if}}
{{else}}
{{#if albums}}
<div id="update">
	<ul id="ul" class="list-group">
     {{#each albums}}
        <li id="li-{{id}}" class="list-group-item">
          <a data-href={{id}} id="a-{{id}}" href="">{{this.title}}</a>
        </li>
				{{/each}}
				</ul>
				</div>
{{else}}
<p>No albums Yet</p>
  {{/if}}
{{/if}}
